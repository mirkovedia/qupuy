# packages/hardhat

Qupuy **no despliega contratos propios**: los tres cursos usan `PublicLock` de
Unlock Protocol desplegados desde su panel (ver el README de la raíz).

Este paquete es el de Scaffold-ETH 2 y se conserva solo por las herramientas
de la cadena local (`yarn chain`, `yarn account`). No tiene contratos, scripts
de despliegue ni tests, y no participa en el build de la aplicación.
