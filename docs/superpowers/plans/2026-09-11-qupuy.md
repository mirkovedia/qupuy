# Qupuy — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Portal de cursos donde el acceso es una membresía de Unlock (NFT ERC-721 con expiración) que el usuario puede transferir a otra persona.

**Architecture:** Frontend Next.js (App Router) sobre Scaffold-ETH 2. Cero contratos propios: se usa el PublicLock de Unlock tal cual. El contenido vive tras una interfaz `ContentRepository` (implementación estática hoy, base de datos después). Toda lectura/escritura onchain se encapsula en hooks; ningún componente de presentación toca un contrato.

**Tech Stack:** Next.js 16 (App Router) · React 19 · TypeScript 5.8 · wagmi 2.19 · viem 2.53 · RainbowKit 2.2 · Tailwind 4 + DaisyUI 5 · Vitest (solo lógica pura)

**Spec:** `docs/superpowers/specs/2026-09-11-portal-cursos-unlock-design.md`

## Global Constraints

- **TypeScript estricto.** Prohibido `any`. Usar `unknown` + type guards si hace falta.
- **`type` sobre `interface`**, salvo `ContentRepository` (interfaz extensible a propósito).
- **Nombres de identificadores en inglés**; textos de UI en español boliviano.
- **Vocabulario de UI:** decir "acceso", nunca "NFT" ni "Key". Precios en "Bs".
- **`getHasValidKey`, NUNCA `balanceOf`** para determinar acceso (spec §6).
- **Gating real:** el contenido protegido no se renderiza sin acceso; prohibido ocultar con CSS (spec §7).
- **Componentes web3** desde `@scaffold-ui/components`, no desde `~~/components/scaffold-eth`.
- **DaisyUI con colores semánticos** (`bg-base-100`, `text-base-content`, `btn-primary`). Prohibido Tailwind crudo de color (`bg-blue-500`).
- **Server Components por defecto**; `"use client"` solo donde haya wallet, estado o eventos.
- **No tocar:** `packages/nextjs/hooks/scaffold-eth/`, `packages/nextjs/components/scaffold-eth/`, `packages/nextjs/contracts/deployedContracts.ts`.
- **Red de desarrollo:** Base Sepolia (chainId 84532).
- **Commits en español**, formato conventional commits (`feat:`, `fix:`, `chore:`).

## Nota sobre testing

El paquete `nextjs` no trae runner de tests. El spec (§12) decidió no añadir tests
automatizados de UI: no aportan a la rúbrica del bounty en el tiempo disponible.

**Este plan instala Vitest solo para lógica pura** (Tarea 1): funciones sin DOM,
sin wallet, sin red. Ahí TDD es barato y previene bugs reales (cálculo de
expiración, resolución de Locks, repositorio de contenido).

Los componentes de UI y los flujos onchain se verifican con la **lista manual**
de la Tarea 12, que replica el spec §12.

---

## Estructura de archivos

| Archivo | Responsabilidad |
| ------- | --------------- |
| `types/curso.ts` | Tipos de dominio: `Curso`, `Modulo` |
| `services/content/types.ts` | Interfaz `ContentRepository` |
| `services/content/staticRepository.ts` | Implementación sobre `data/cursos.ts` |
| `services/content/index.ts` | Punto único de intercambio de implementación |
| `data/cursos.ts` | Contenido de los cursos |
| `contracts/unlock/publicLockAbi.ts` | ABI mínimo del PublicLock |
| `contracts/unlock/locks.ts` | Registro de Locks por red + `resolverLock` |
| `utils/membresia.ts` | Lógica pura: `calcularDiasRestantes` |
| `hooks/useMembresia.ts` | Lectura onchain del estado de acceso |
| `hooks/useComprarAcceso.ts` | Escritura: `purchase` |
| `hooks/useTransferirAcceso.ts` | Escritura: `transferFrom` (P1) |
| `components/cursos/CursoCard.tsx` | Tarjeta del catálogo |
| `components/cursos/EstadoMembresia.tsx` | Badge de acceso activo/vencido |
| `components/cursos/ListaModulos.tsx` | Lista con candados |
| `components/cursos/ReproductorVideo.tsx` | Reproductor |
| `components/cursos/BotonDesbloquear.tsx` | Compra |
| `components/cursos/ModalTransferir.tsx` | Transferencia (P1) |
| `app/page.tsx` | Catálogo |
| `app/curso/[slug]/page.tsx` | Detalle con los dos estados |
| `app/mi-acceso/page.tsx` | Accesos del usuario (P1) |

---

# FASE P0 — Flujo obligatorio del bounty

---

### Task 1: Infraestructura de tipos, contenido y tests

Sienta la base de datos del dominio y el runner de tests para lógica pura.

**Files:**
- Create: `packages/nextjs/types/curso.ts`
- Create: `packages/nextjs/services/content/types.ts`
- Create: `packages/nextjs/services/content/staticRepository.ts`
- Create: `packages/nextjs/services/content/index.ts`
- Create: `packages/nextjs/data/cursos.ts`
- Create: `packages/nextjs/vitest.config.ts`
- Create: `packages/nextjs/services/content/staticRepository.test.ts`
- Modify: `packages/nextjs/package.json`

**Interfaces:**
- Produces: `type Curso`, `type Modulo`, `interface ContentRepository`, `contentRepository` (instancia), `CURSOS` (array)

- [ ] **Step 1: Instalar Vitest**

```bash
cd packages/nextjs && yarn add -D vitest@^3.2.4
```

- [ ] **Step 2: Configurar Vitest**

Crear `packages/nextjs/vitest.config.ts`:

```typescript
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    environment: "node",
    include: ["**/*.test.ts"],
  },
});
```

Añadir a `packages/nextjs/package.json` en `scripts`:

```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 3: Definir los tipos de dominio**

Crear `packages/nextjs/types/curso.ts`:

```typescript
export type Modulo = {
  id: string;
  titulo: string;
  duracionSegundos: number;
  videoUrl: string;
  /** El módulo de vista previa, accesible sin membresía */
  esGratuito: boolean;
};

export type Creador = {
  nombre: string;
  ciudad: string;
};

export type Curso = {
  id: string;
  slug: string;
  titulo: string;
  descripcion: string;
  creador: Creador;
  portadaUrl: string;
  /** Precio de referencia mostrado al usuario, en bolivianos */
  precioBs: number;
  duracionDias: number;
  /** Clave que resuelve la dirección del Lock en LOCKS_POR_RED */
  lockKey: string;
  modulos: Modulo[];
};
```

- [ ] **Step 4: Definir la interfaz del repositorio**

Crear `packages/nextjs/services/content/types.ts`:

```typescript
import type { Curso } from "~~/types/curso";

/**
 * Fuente de contenido del portal. Hoy se sirve desde un archivo estático;
 * migrar a base de datos o CMS significa escribir otra implementación de
 * esta interfaz y cambiarla en services/content/index.ts.
 */
export interface ContentRepository {
  listarCursos(): Promise<Curso[]>;
  obtenerCursoPorSlug(slug: string): Promise<Curso | null>;
}
```

- [ ] **Step 5: Escribir el contenido de los cursos**

Crear `packages/nextjs/data/cursos.ts`. Tres cursos, cada uno con un módulo
gratuito primero:

```typescript
import type { Curso } from "~~/types/curso";

export const CURSOS: Curso[] = [
  {
    id: "1",
    slug: "ingles-basico",
    titulo: "Inglés desde cero",
    descripcion:
      "Aprende a presentarte, hacer preguntas y sostener una conversación básica en inglés. Sin gramática aburrida.",
    creador: { nombre: "María Quispe", ciudad: "Cochabamba" },
    portadaUrl: "/cursos/ingles.jpg",
    precioBs: 70,
    duracionDias: 30,
    lockKey: "ingles-basico",
    modulos: [
      {
        id: "1-1",
        titulo: "Presentarte en inglés",
        duracionSegundos: 120,
        videoUrl: "/videos/ingles-01.mp4",
        esGratuito: true,
      },
      {
        id: "1-2",
        titulo: "El verbo to be",
        duracionSegundos: 480,
        videoUrl: "/videos/ingles-02.mp4",
        esGratuito: false,
      },
      {
        id: "1-3",
        titulo: "Presente simple",
        duracionSegundos: 540,
        videoUrl: "/videos/ingles-03.mp4",
        esGratuito: false,
      },
      {
        id: "1-4",
        titulo: "Vocabulario del día a día",
        duracionSegundos: 420,
        videoUrl: "/videos/ingles-04.mp4",
        esGratuito: false,
      },
    ],
  },
  {
    id: "2",
    slug: "excel-negocios",
    titulo: "Excel para tu negocio",
    descripcion:
      "Lleva el control de tu tienda o emprendimiento: inventario, ventas y ganancias en planillas simples.",
    creador: { nombre: "Jorge Mamani", ciudad: "La Paz" },
    portadaUrl: "/cursos/excel.jpg",
    precioBs: 50,
    duracionDias: 30,
    lockKey: "excel-negocios",
    modulos: [
      {
        id: "2-1",
        titulo: "Tu primera planilla de ventas",
        duracionSegundos: 150,
        videoUrl: "/videos/excel-01.mp4",
        esGratuito: true,
      },
      {
        id: "2-2",
        titulo: "Fórmulas que sí vas a usar",
        duracionSegundos: 600,
        videoUrl: "/videos/excel-02.mp4",
        esGratuito: false,
      },
      {
        id: "2-3",
        titulo: "Control de inventario",
        duracionSegundos: 520,
        videoUrl: "/videos/excel-03.mp4",
        esGratuito: false,
      },
    ],
  },
  {
    id: "3",
    slug: "reparacion-celulares",
    titulo: "Reparación de celulares",
    descripcion:
      "Cambio de pantalla, batería y diagnóstico de fallas comunes. Empieza a cobrar por arreglar equipos.",
    creador: { nombre: "Luis Choque", ciudad: "Santa Cruz" },
    portadaUrl: "/cursos/celulares.jpg",
    precioBs: 120,
    duracionDias: 30,
    lockKey: "reparacion-celulares",
    modulos: [
      {
        id: "3-1",
        titulo: "Herramientas que necesitas",
        duracionSegundos: 180,
        videoUrl: "/videos/celulares-01.mp4",
        esGratuito: true,
      },
      {
        id: "3-2",
        titulo: "Cambiar una pantalla",
        duracionSegundos: 720,
        videoUrl: "/videos/celulares-02.mp4",
        esGratuito: false,
      },
      {
        id: "3-3",
        titulo: "Reemplazo de batería",
        duracionSegundos: 480,
        videoUrl: "/videos/celulares-03.mp4",
        esGratuito: false,
      },
    ],
  },
];
```

- [ ] **Step 6: Escribir el test que falla**

Crear `packages/nextjs/services/content/staticRepository.test.ts`:

```typescript
import { describe, expect, it } from "vitest";
import { staticContentRepository } from "./staticRepository";

describe("staticContentRepository", () => {
  it("lista todos los cursos", async () => {
    const cursos = await staticContentRepository.listarCursos();
    expect(cursos).toHaveLength(3);
  });

  it("obtiene un curso por su slug", async () => {
    const curso = await staticContentRepository.obtenerCursoPorSlug("ingles-basico");
    expect(curso?.titulo).toBe("Inglés desde cero");
  });

  it("devuelve null si el slug no existe", async () => {
    const curso = await staticContentRepository.obtenerCursoPorSlug("no-existe");
    expect(curso).toBeNull();
  });

  it("cada curso tiene exactamente un módulo gratuito", async () => {
    const cursos = await staticContentRepository.listarCursos();
    for (const curso of cursos) {
      const gratuitos = curso.modulos.filter(m => m.esGratuito);
      expect(gratuitos).toHaveLength(1);
    }
  });

  it("el módulo gratuito es siempre el primero", async () => {
    const cursos = await staticContentRepository.listarCursos();
    for (const curso of cursos) {
      expect(curso.modulos[0].esGratuito).toBe(true);
    }
  });
});
```

- [ ] **Step 7: Ejecutar el test y verificar que falla**

Run: `cd packages/nextjs && yarn test`
Expected: FAIL — `Cannot find module './staticRepository'`

- [ ] **Step 8: Implementar el repositorio**

Crear `packages/nextjs/services/content/staticRepository.ts`:

```typescript
import type { ContentRepository } from "./types";
import { CURSOS } from "~~/data/cursos";

export const staticContentRepository: ContentRepository = {
  async listarCursos() {
    return CURSOS;
  },

  async obtenerCursoPorSlug(slug: string) {
    return CURSOS.find(curso => curso.slug === slug) ?? null;
  },
};
```

Crear `packages/nextjs/services/content/index.ts`:

```typescript
import { staticContentRepository } from "./staticRepository";
import type { ContentRepository } from "./types";

/**
 * Punto único de intercambio de la fuente de contenido.
 * Para migrar a base de datos, reemplazar esta asignación.
 */
export const contentRepository: ContentRepository = staticContentRepository;

export type { ContentRepository };
```

- [ ] **Step 9: Ejecutar el test y verificar que pasa**

Run: `cd packages/nextjs && yarn test`
Expected: PASS — 5 tests

- [ ] **Step 10: Commit**

```bash
git add packages/nextjs/types packages/nextjs/services packages/nextjs/data packages/nextjs/vitest.config.ts packages/nextjs/package.json
git commit -m "feat: añadir tipos de dominio y repositorio de contenido"
```

---

### Task 2: ABI de Unlock y registro de Locks

**Files:**
- Create: `packages/nextjs/contracts/unlock/publicLockAbi.ts`
- Create: `packages/nextjs/contracts/unlock/locks.ts`
- Create: `packages/nextjs/contracts/unlock/locks.test.ts`
- Create: `packages/nextjs/.env.example`

**Interfaces:**
- Consumes: nada
- Produces: `PUBLIC_LOCK_ABI`, `resolverLock(lockKey: string, chainId: number): Address | undefined`, `LOCKS_POR_RED`

- [ ] **Step 1: Crear el ABI mínimo**

Crear `packages/nextjs/contracts/unlock/publicLockAbi.ts`. Solo las funciones
que usa el portal:

```typescript
/**
 * ABI mínimo del contrato PublicLock de Unlock Protocol.
 * Solo incluye las funciones que usa Qupuy.
 * Referencia: https://docs.unlock-protocol.com/core-protocol/smart-contracts-api/publiclock/
 */
export const PUBLIC_LOCK_ABI = [
  {
    inputs: [{ internalType: "address", name: "_keyOwner", type: "address" }],
    name: "getHasValidKey",
    outputs: [{ internalType: "bool", name: "", type: "bool" }],
    stateMutability: "view",
    type: "function",
  },
  {
    inputs: [{ internalType: "address", name: "_keyOwner", type: "address" }],
    name: "balanceOf",
    outputs: [{ internalType: "uint256", name: "balance", type: "uint256" }],
    stateMutability: "view",
    type: "function",
  },
  {
    inputs: [
      { internalType: "address", name: "_keyOwner", type: "address" },
      { internalType: "uint256", name: "_index", type: "uint256" },
    ],
    name: "tokenOfOwnerByIndex",
    outputs: [{ internalType: "uint256", name: "", type: "uint256" }],
    stateMutability: "view",
    type: "function",
  },
  {
    inputs: [{ internalType: "uint256", name: "_tokenId", type: "uint256" }],
    name: "keyExpirationTimestampFor",
    outputs: [{ internalType: "uint256", name: "", type: "uint256" }],
    stateMutability: "view",
    type: "function",
  },
  {
    inputs: [],
    name: "keyPrice",
    outputs: [{ internalType: "uint256", name: "", type: "uint256" }],
    stateMutability: "view",
    type: "function",
  },
  {
    inputs: [],
    name: "tokenAddress",
    outputs: [{ internalType: "address", name: "", type: "address" }],
    stateMutability: "view",
    type: "function",
  },
  {
    inputs: [
      { internalType: "uint256[]", name: "_values", type: "uint256[]" },
      { internalType: "address[]", name: "_recipients", type: "address[]" },
      { internalType: "address[]", name: "_referrers", type: "address[]" },
      { internalType: "address[]", name: "_keyManagers", type: "address[]" },
      { internalType: "bytes[]", name: "_data", type: "bytes[]" },
    ],
    name: "purchase",
    outputs: [{ internalType: "uint256[]", name: "", type: "uint256[]" }],
    stateMutability: "payable",
    type: "function",
  },
  {
    inputs: [
      { internalType: "address", name: "_from", type: "address" },
      { internalType: "address", name: "_to", type: "address" },
      { internalType: "uint256", name: "_tokenId", type: "uint256" },
    ],
    name: "transferFrom",
    outputs: [],
    stateMutability: "nonpayable",
    type: "function",
  },
] as const;
```

- [ ] **Step 2: Escribir el test que falla**

Crear `packages/nextjs/contracts/unlock/locks.test.ts`:

```typescript
import { describe, expect, it } from "vitest";
import { resolverLock } from "./locks";

const BASE_SEPOLIA = 84532;

describe("resolverLock", () => {
  it("devuelve la dirección de un Lock registrado", () => {
    const address = resolverLock("ingles-basico", BASE_SEPOLIA);
    expect(address).toMatch(/^0x[a-fA-F0-9]{40}$/);
  });

  it("devuelve undefined si el lockKey no existe", () => {
    expect(resolverLock("curso-inventado", BASE_SEPOLIA)).toBeUndefined();
  });

  it("devuelve undefined si la red no está soportada", () => {
    expect(resolverLock("ingles-basico", 999999)).toBeUndefined();
  });
});
```

- [ ] **Step 3: Ejecutar el test y verificar que falla**

Run: `cd packages/nextjs && yarn test`
Expected: FAIL — `Cannot find module './locks'`

- [ ] **Step 4: Implementar el registro de Locks**

Crear `packages/nextjs/contracts/unlock/locks.ts`:

```typescript
import type { Address } from "viem";
import { baseSepolia } from "viem/chains";

/**
 * Dirección placeholder mientras no se despliegan los Locks reales.
 * Se sustituye por variables de entorno en la Tarea 11.
 */
const PENDIENTE = "0x0000000000000000000000000000000000000000" as Address;

const leerLock = (variable: string | undefined): Address =>
  (variable as Address | undefined) ?? PENDIENTE;

/**
 * Registro de Locks por red. Añadir una red es añadir una entrada aquí;
 * no requiere tocar componentes ni hooks.
 */
export const LOCKS_POR_RED: Record<number, Record<string, Address>> = {
  [baseSepolia.id]: {
    "ingles-basico": leerLock(process.env.NEXT_PUBLIC_LOCK_INGLES),
    "excel-negocios": leerLock(process.env.NEXT_PUBLIC_LOCK_EXCEL),
    "reparacion-celulares": leerLock(process.env.NEXT_PUBLIC_LOCK_CELULARES),
  },
};

/** Resuelve la dirección del Lock de un curso en la red indicada. */
export const resolverLock = (lockKey: string, chainId: number): Address | undefined =>
  LOCKS_POR_RED[chainId]?.[lockKey];
```

- [ ] **Step 5: Crear el archivo de ejemplo de variables de entorno**

Crear `packages/nextjs/.env.example`:

```
# Locks de Unlock desplegados en Base Sepolia (chainId 84532)
NEXT_PUBLIC_LOCK_INGLES=0x
NEXT_PUBLIC_LOCK_EXCEL=0x
NEXT_PUBLIC_LOCK_CELULARES=0x

# Opcionales
NEXT_PUBLIC_ALCHEMY_API_KEY=
NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID=
```

- [ ] **Step 6: Ejecutar el test y verificar que pasa**

Run: `cd packages/nextjs && yarn test`
Expected: PASS — 8 tests en total

- [ ] **Step 7: Commit**

```bash
git add packages/nextjs/contracts/unlock packages/nextjs/.env.example
git commit -m "feat: añadir ABI de PublicLock y registro de Locks por red"
```

---

### Task 3: Lógica pura de membresía

Separar el cálculo de expiración de la lectura onchain permite testearlo sin red.

**Files:**
- Create: `packages/nextjs/utils/membresia.ts`
- Create: `packages/nextjs/utils/membresia.test.ts`

**Interfaces:**
- Produces: `calcularDiasRestantes(expiracion: bigint, ahora?: Date): number`, `type EstadoAcceso`, `determinarEstado(...)`

- [ ] **Step 1: Escribir el test que falla**

Crear `packages/nextjs/utils/membresia.test.ts`:

```typescript
import { describe, expect, it } from "vitest";
import { calcularDiasRestantes, determinarEstado } from "./membresia";

const ahora = new Date("2026-09-11T12:00:00Z");
const enSegundos = (fecha: Date) => BigInt(Math.floor(fecha.getTime() / 1000));

describe("calcularDiasRestantes", () => {
  it("calcula los días completos que faltan", () => {
    const dentroDe10Dias = new Date("2026-09-21T12:00:00Z");
    expect(calcularDiasRestantes(enSegundos(dentroDe10Dias), ahora)).toBe(10);
  });

  it("devuelve 0 si ya expiró", () => {
    const hace3Dias = new Date("2026-09-08T12:00:00Z");
    expect(calcularDiasRestantes(enSegundos(hace3Dias), ahora)).toBe(0);
  });

  it("devuelve 0 si expira exactamente ahora", () => {
    expect(calcularDiasRestantes(enSegundos(ahora), ahora)).toBe(0);
  });

  it("redondea hacia abajo las fracciones de día", () => {
    const dentroDe36Horas = new Date("2026-09-13T00:00:00Z");
    expect(calcularDiasRestantes(enSegundos(dentroDe36Horas), ahora)).toBe(1);
  });
});

describe("determinarEstado", () => {
  it("es 'activo' cuando el contrato reporta key válida", () => {
    expect(determinarEstado(true, 28)).toBe("activo");
  });

  it("es 'vencido' cuando tuvo key pero ya no es válida", () => {
    expect(determinarEstado(false, 0)).toBe("vencido");
  });

  it("es 'sin-acceso' cuando nunca tuvo key", () => {
    expect(determinarEstado(false, undefined)).toBe("sin-acceso");
  });
});
```

- [ ] **Step 2: Ejecutar el test y verificar que falla**

Run: `cd packages/nextjs && yarn test`
Expected: FAIL — `Cannot find module './membresia'`

- [ ] **Step 3: Implementar la lógica**

Crear `packages/nextjs/utils/membresia.ts`:

```typescript
const SEGUNDOS_POR_DIA = 86_400;

/**
 * Días completos que faltan para que expire una membresía.
 * Devuelve 0 si ya expiró.
 *
 * @param expiracion timestamp UNIX en segundos, tal como lo devuelve
 *                   keyExpirationTimestampFor
 */
export const calcularDiasRestantes = (expiracion: bigint, ahora: Date = new Date()): number => {
  const ahoraEnSegundos = BigInt(Math.floor(ahora.getTime() / 1000));
  if (expiracion <= ahoraEnSegundos) return 0;
  return Number((expiracion - ahoraEnSegundos) / BigInt(SEGUNDOS_POR_DIA));
};

export type EstadoAcceso = "activo" | "vencido" | "sin-acceso";

/**
 * Traduce la respuesta del contrato a un estado de UI.
 *
 * @param tieneKeyValida resultado de getHasValidKey
 * @param diasRestantes  undefined si el usuario nunca tuvo una key
 */
export const determinarEstado = (tieneKeyValida: boolean, diasRestantes: number | undefined): EstadoAcceso => {
  if (tieneKeyValida) return "activo";
  if (diasRestantes !== undefined) return "vencido";
  return "sin-acceso";
};
```

- [ ] **Step 4: Ejecutar el test y verificar que pasa**

Run: `cd packages/nextjs && yarn test`
Expected: PASS — 15 tests en total

- [ ] **Step 5: Commit**

```bash
git add packages/nextjs/utils/membresia.ts packages/nextjs/utils/membresia.test.ts
git commit -m "feat: añadir lógica de cálculo de estado de membresía"
```

---

### Task 4: Configurar Base Sepolia como red objetivo

**Files:**
- Modify: `packages/nextjs/scaffold.config.ts`

- [ ] **Step 1: Cambiar la red objetivo**

En `packages/nextjs/scaffold.config.ts`, sustituir la línea de `targetNetworks`:

```typescript
// Antes
targetNetworks: [chains.hardhat],

// Después
targetNetworks: [chains.baseSepolia],
```

- [ ] **Step 2: Verificar que el proyecto compila**

Run: `cd packages/nextjs && yarn check-types`
Expected: sin errores

- [ ] **Step 3: Verificar que el frontend arranca**

Run: `yarn start`
Expected: el servidor levanta en `http://localhost:3000`, el selector de red muestra Base Sepolia

- [ ] **Step 4: Commit**

```bash
git add packages/nextjs/scaffold.config.ts
git commit -m "chore: apuntar la app a Base Sepolia"
```

---

### Task 5: Hook de lectura de membresía

El único lugar de la app que sabe cómo se consulta el acceso onchain.

**Files:**
- Create: `packages/nextjs/hooks/useMembresia.ts`

**Interfaces:**
- Consumes: `resolverLock`, `PUBLIC_LOCK_ABI` (Task 2); `calcularDiasRestantes`, `determinarEstado`, `EstadoAcceso` (Task 3)
- Produces: `useMembresia(lockKey: string)` → `{ estado: EstadoAcceso; tieneAcceso: boolean; diasRestantes: number | undefined; tokenId: bigint | undefined; lockAddress: Address | undefined; isLoading: boolean; refetch: () => void }`

- [ ] **Step 1: Implementar el hook**

Crear `packages/nextjs/hooks/useMembresia.ts`:

```typescript
"use client";

import type { Address } from "viem";
import { useAccount, useChainId, useReadContract } from "wagmi";
import { PUBLIC_LOCK_ABI } from "~~/contracts/unlock/publicLockAbi";
import { resolverLock } from "~~/contracts/unlock/locks";
import { calcularDiasRestantes, determinarEstado, type EstadoAcceso } from "~~/utils/membresia";

export type Membresia = {
  estado: EstadoAcceso;
  tieneAcceso: boolean;
  diasRestantes: number | undefined;
  tokenId: bigint | undefined;
  lockAddress: Address | undefined;
  isLoading: boolean;
  refetch: () => void;
};

/**
 * Estado de acceso del usuario conectado para un curso.
 *
 * IMPORTANTE: usa getHasValidKey, no balanceOf. balanceOf cuenta también las
 * membresías vencidas, porque el NFT permanece en la wallet tras expirar.
 */
export const useMembresia = (lockKey: string): Membresia => {
  const { address } = useAccount();
  const chainId = useChainId();
  const lockAddress = resolverLock(lockKey, chainId);
  const habilitado = Boolean(lockAddress && address);

  const {
    data: tieneKeyValida,
    isLoading: cargandoValidez,
    refetch: refetchValidez,
  } = useReadContract({
    address: lockAddress,
    abi: PUBLIC_LOCK_ABI,
    functionName: "getHasValidKey",
    args: address ? [address] : undefined,
    query: { enabled: habilitado },
  });

  const { data: balance, refetch: refetchBalance } = useReadContract({
    address: lockAddress,
    abi: PUBLIC_LOCK_ABI,
    functionName: "balanceOf",
    args: address ? [address] : undefined,
    query: { enabled: habilitado },
  });

  const poseeAlgunaKey = (balance ?? 0n) > 0n;

  const { data: tokenId, refetch: refetchToken } = useReadContract({
    address: lockAddress,
    abi: PUBLIC_LOCK_ABI,
    functionName: "tokenOfOwnerByIndex",
    args: address ? [address, 0n] : undefined,
    query: { enabled: habilitado && poseeAlgunaKey },
  });

  const { data: expiracion, refetch: refetchExpiracion } = useReadContract({
    address: lockAddress,
    abi: PUBLIC_LOCK_ABI,
    functionName: "keyExpirationTimestampFor",
    args: tokenId !== undefined ? [tokenId] : undefined,
    query: { enabled: habilitado && tokenId !== undefined },
  });

  const diasRestantes = expiracion !== undefined ? calcularDiasRestantes(expiracion) : undefined;
  const tieneAcceso = tieneKeyValida === true;

  return {
    estado: determinarEstado(tieneAcceso, diasRestantes),
    tieneAcceso,
    diasRestantes,
    tokenId,
    lockAddress,
    isLoading: cargandoValidez,
    refetch: () => {
      void refetchValidez();
      void refetchBalance();
      void refetchToken();
      void refetchExpiracion();
    },
  };
};
```

- [ ] **Step 2: Verificar tipos**

Run: `cd packages/nextjs && yarn check-types`
Expected: sin errores

- [ ] **Step 3: Commit**

```bash
git add packages/nextjs/hooks/useMembresia.ts
git commit -m "feat: añadir hook de lectura de estado de membresía"
```

---

### Task 6: Componentes de presentación del curso

Componentes sin lógica onchain: reciben props y renderizan.

**Files:**
- Create: `packages/nextjs/components/cursos/EstadoMembresia.tsx`
- Create: `packages/nextjs/components/cursos/ReproductorVideo.tsx`
- Create: `packages/nextjs/components/cursos/ListaModulos.tsx`
- Create: `packages/nextjs/components/cursos/CursoCard.tsx`

**Interfaces:**
- Consumes: `Curso`, `Modulo` (Task 1); `EstadoAcceso` (Task 3)
- Produces: `<EstadoMembresia>`, `<ReproductorVideo>`, `<ListaModulos>`, `<CursoCard>`

- [ ] **Step 1: Badge de estado**

Crear `packages/nextjs/components/cursos/EstadoMembresia.tsx`:

```typescript
import type { EstadoAcceso } from "~~/utils/membresia";

type Props = {
  estado: EstadoAcceso;
  diasRestantes?: number;
};

export const EstadoMembresia = ({ estado, diasRestantes }: Props) => {
  if (estado === "activo") {
    return (
      <div className="badge badge-success gap-2">
        ✓ Acceso activo
        {diasRestantes !== undefined && <span>· vence en {diasRestantes} días</span>}
      </div>
    );
  }

  if (estado === "vencido") {
    return <div className="badge badge-warning gap-2">Tu acceso venció</div>;
  }

  return <div className="badge badge-ghost gap-2">🔒 Sin acceso</div>;
};
```

- [ ] **Step 2: Reproductor**

Crear `packages/nextjs/components/cursos/ReproductorVideo.tsx`:

```typescript
"use client";

type Props = {
  src: string;
  titulo: string;
  esVistaPrevia?: boolean;
};

export const ReproductorVideo = ({ src, titulo, esVistaPrevia = false }: Props) => (
  <div className="w-full">
    <video
      key={src}
      src={src}
      controls
      className="w-full rounded-box bg-base-300 aspect-video"
      aria-label={titulo}
    />
    <div className="flex items-center justify-between mt-2">
      <p className="font-medium">{titulo}</p>
      {esVistaPrevia && <span className="badge badge-outline badge-sm">Vista previa gratuita</span>}
    </div>
  </div>
);
```

- [ ] **Step 3: Lista de módulos**

Crear `packages/nextjs/components/cursos/ListaModulos.tsx`:

```typescript
"use client";

import type { Modulo } from "~~/types/curso";

const formatearDuracion = (segundos: number): string => {
  const minutos = Math.floor(segundos / 60);
  const resto = segundos % 60;
  return `${minutos}:${resto.toString().padStart(2, "0")}`;
};

type Props = {
  modulos: Modulo[];
  tieneAcceso: boolean;
  moduloActivoId: string;
  onSeleccionar: (modulo: Modulo) => void;
};

export const ListaModulos = ({ modulos, tieneAcceso, moduloActivoId, onSeleccionar }: Props) => (
  <ul className="menu bg-base-200 rounded-box w-full">
    <li className="menu-title">Contenido del curso</li>
    {modulos.map((modulo, indice) => {
      const desbloqueado = tieneAcceso || modulo.esGratuito;
      const esActivo = modulo.id === moduloActivoId;

      return (
        <li key={modulo.id}>
          <button
            type="button"
            disabled={!desbloqueado}
            onClick={() => onSeleccionar(modulo)}
            className={`flex justify-between ${esActivo ? "active" : ""} ${
              desbloqueado ? "" : "opacity-60 cursor-not-allowed"
            }`}
          >
            <span className="flex gap-3 items-center text-left">
              <span className="text-base-content/50">{indice + 1}.</span>
              <span>{modulo.titulo}</span>
            </span>
            <span className="flex gap-2 items-center shrink-0">
              <span className="text-xs text-base-content/60">{formatearDuracion(modulo.duracionSegundos)}</span>
              {desbloqueado ? (
                modulo.esGratuito && !tieneAcceso ? (
                  <span className="badge badge-outline badge-xs">gratis</span>
                ) : null
              ) : (
                <span aria-label="Bloqueado">🔒</span>
              )}
            </span>
          </button>
        </li>
      );
    })}
  </ul>
);
```

- [ ] **Step 4: Tarjeta del catálogo**

Crear `packages/nextjs/components/cursos/CursoCard.tsx`:

```typescript
"use client";

import Link from "next/link";
import { EstadoMembresia } from "./EstadoMembresia";
import type { Curso } from "~~/types/curso";
import { useMembresia } from "~~/hooks/useMembresia";

type Props = {
  curso: Curso;
};

export const CursoCard = ({ curso }: Props) => {
  const { estado, diasRestantes } = useMembresia(curso.lockKey);

  return (
    <Link href={`/curso/${curso.slug}`} className="card bg-base-100 shadow-xl hover:shadow-2xl transition-shadow">
      <figure className="aspect-video bg-base-300">
        {/* eslint-disable-next-line @next/next/no-img-element */}
        <img src={curso.portadaUrl} alt={curso.titulo} className="w-full h-full object-cover" />
      </figure>
      <div className="card-body">
        <h2 className="card-title">{curso.titulo}</h2>
        <p className="text-sm text-base-content/70">
          {curso.creador.nombre} · {curso.creador.ciudad}
        </p>
        <p className="text-sm line-clamp-2">{curso.descripcion}</p>
        <div className="card-actions justify-between items-center mt-2">
          <span className="font-bold text-lg">Bs {curso.precioBs}</span>
          <EstadoMembresia estado={estado} diasRestantes={diasRestantes} />
        </div>
      </div>
    </Link>
  );
};
```

- [ ] **Step 5: Verificar tipos y lint**

Run: `cd packages/nextjs && yarn check-types && yarn lint`
Expected: sin errores

- [ ] **Step 6: Commit**

```bash
git add packages/nextjs/components/cursos
git commit -m "feat: añadir componentes de presentación de cursos"
```

---

### Task 7: Página de catálogo

**Files:**
- Modify: `packages/nextjs/app/page.tsx` (reescritura completa)
- Modify: `packages/nextjs/app/layout.tsx:8-11`
- Modify: `packages/nextjs/components/Header.tsx:18-28`

**Interfaces:**
- Consumes: `contentRepository` (Task 1), `<CursoCard>` (Task 6)

- [ ] **Step 1: Reescribir la página de inicio**

Reemplazar todo el contenido de `packages/nextjs/app/page.tsx`:

```typescript
import type { NextPage } from "next";
import { CursoCard } from "~~/components/cursos/CursoCard";
import { contentRepository } from "~~/services/content";

const Home: NextPage = async () => {
  const cursos = await contentRepository.listarCursos();

  return (
    <div className="container mx-auto px-4 py-10">
      <header className="text-center mb-12">
        <h1 className="text-4xl font-bold mb-3">Aprende sin fronteras</h1>
        <p className="text-lg text-base-content/70 max-w-2xl mx-auto">
          Cursos de creadores bolivianos. Tu acceso te pertenece — y cuando termines, se lo puedes pasar a alguien.
        </p>
      </header>

      <div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
        {cursos.map(curso => (
          <CursoCard key={curso.id} curso={curso} />
        ))}
      </div>
    </div>
  );
};

export default Home;
```

- [ ] **Step 2: Actualizar los metadatos del sitio**

En `packages/nextjs/app/layout.tsx`, reemplazar el bloque `getMetadata`:

```typescript
export const metadata = getMetadata({
  title: "Qupuy — Cursos con acceso transferible",
  description: "Compra el curso, apréndelo y pásale el acceso a alguien más.",
});
```

- [ ] **Step 3: Añadir la navegación**

En `packages/nextjs/components/Header.tsx`, reemplazar el array `menuLinks`:

```typescript
export const menuLinks: HeaderMenuLink[] = [
  {
    label: "Cursos",
    href: "/",
  },
  {
    label: "Mi acceso",
    href: "/mi-acceso",
  },
  {
    label: "Debug Contracts",
    href: "/debug",
    icon: <BugAntIcon className="h-4 w-4" />,
  },
];
```

- [ ] **Step 4: Añadir imágenes de portada**

Crear `packages/nextjs/public/cursos/` con tres imágenes: `ingles.jpg`,
`excel.jpg`, `celulares.jpg`. Sirven imágenes libres de derechos o un color
sólido temporal — la portada no es evaluada, pero su ausencia se nota.

- [ ] **Step 5: Verificar en el navegador**

Run: `yarn start`
Expected: `http://localhost:3000` muestra tres tarjetas de curso. Sin wallet
conectada, cada una muestra el badge "Sin acceso".

- [ ] **Step 6: Commit**

```bash
git add packages/nextjs/app/page.tsx packages/nextjs/app/layout.tsx packages/nextjs/components/Header.tsx packages/nextjs/public/cursos
git commit -m "feat: añadir página de catálogo de cursos"
```

---

### Task 8: Hook de compra de acceso

**Files:**
- Create: `packages/nextjs/hooks/useComprarAcceso.ts`

**Interfaces:**
- Consumes: `PUBLIC_LOCK_ABI` (Task 2)
- Produces: `useComprarAcceso(lockAddress)` → `{ comprar: () => Promise<void>; isPending: boolean; precio: bigint | undefined }`

- [ ] **Step 1: Implementar el hook**

Crear `packages/nextjs/hooks/useComprarAcceso.ts`:

```typescript
"use client";

import { useState } from "react";
import type { Address } from "viem";
import { useAccount, usePublicClient, useReadContract, useWriteContract } from "wagmi";
import { PUBLIC_LOCK_ABI } from "~~/contracts/unlock/publicLockAbi";
import { notification, getParsedError } from "~~/utils/scaffold-eth";

const DIRECCION_CERO = "0x0000000000000000000000000000000000000000" as Address;

/**
 * Compra de una membresía en un Lock de Unlock.
 * Solo soporta Locks con precio en moneda nativa (ETH), que es la
 * configuración usada por Qupuy.
 */
export const useComprarAcceso = (lockAddress: Address | undefined) => {
  const { address } = useAccount();
  const publicClient = usePublicClient();
  const { writeContractAsync } = useWriteContract();
  const [isPending, setIsPending] = useState(false);

  const { data: precio } = useReadContract({
    address: lockAddress,
    abi: PUBLIC_LOCK_ABI,
    functionName: "keyPrice",
    query: { enabled: Boolean(lockAddress) },
  });

  const comprar = async (): Promise<void> => {
    if (!lockAddress || !address || precio === undefined) {
      notification.error("Conecta tu wallet para comprar el acceso");
      return;
    }

    setIsPending(true);
    try {
      const hash = await writeContractAsync({
        address: lockAddress,
        abi: PUBLIC_LOCK_ABI,
        functionName: "purchase",
        // _data es bytes[]: un elemento por comprador, NO un array anidado.
  // La forma [["0x"]] falla en runtime ("hex_.replace is not a function").
  args: [[precio], [address], [DIRECCION_CERO], [DIRECCION_CERO], ["0x"]],
        value: precio,
      });

      await publicClient?.waitForTransactionReceipt({ hash });
      notification.success("¡Listo! Ya tienes acceso al curso");
    } catch (error) {
      notification.error(getParsedError(error));
    } finally {
      setIsPending(false);
    }
  };

  return { comprar, isPending, precio };
};
```

- [ ] **Step 2: Verificar tipos**

Run: `cd packages/nextjs && yarn check-types`
Expected: sin errores

- [ ] **Step 3: Commit**

```bash
git add packages/nextjs/hooks/useComprarAcceso.ts
git commit -m "feat: añadir hook de compra de acceso"
```

---

### Task 9: Botón de desbloqueo

**Files:**
- Create: `packages/nextjs/components/cursos/BotonDesbloquear.tsx`

**Interfaces:**
- Consumes: `useComprarAcceso` (Task 8), `Curso` (Task 1)
- Produces: `<BotonDesbloquear curso lockAddress onCompraExitosa />`

- [ ] **Step 1: Implementar el componente**

Crear `packages/nextjs/components/cursos/BotonDesbloquear.tsx`:

```typescript
"use client";

import type { Address } from "viem";
import { formatEther } from "viem";
import { useAccount } from "wagmi";
import type { Curso } from "~~/types/curso";
import { useComprarAcceso } from "~~/hooks/useComprarAcceso";

type Props = {
  curso: Curso;
  lockAddress: Address | undefined;
  onCompraExitosa: () => void;
};

export const BotonDesbloquear = ({ curso, lockAddress, onCompraExitosa }: Props) => {
  const { address } = useAccount();
  const { comprar, isPending, precio } = useComprarAcceso(lockAddress);

  if (!address) {
    return (
      <div className="alert alert-info">
        <span>Conecta tu wallet para desbloquear este curso.</span>
      </div>
    );
  }

  if (!lockAddress) {
    return (
      <div className="alert alert-warning">
        <span>Este curso no está disponible en la red seleccionada.</span>
      </div>
    );
  }

  const manejarCompra = async () => {
    await comprar();
    onCompraExitosa();
  };

  return (
    <div className="card bg-base-200">
      <div className="card-body">
        <button type="button" className="btn btn-primary btn-lg" disabled={isPending} onClick={manejarCompra}>
          {isPending ? (
            <>
              <span className="loading loading-spinner" />
              Procesando…
            </>
          ) : (
            <>🔓 Desbloquear curso completo</>
          )}
        </button>

        <p className="text-center text-sm text-base-content/70">
          Bs {curso.precioBs} · acceso por {curso.duracionDias} días
          {precio !== undefined && <span className="block">({formatEther(precio)} ETH)</span>}
        </p>

        <p className="text-center text-xs text-base-content/60">
          Tu acceso es tuyo: al terminar, se lo puedes pasar a alguien.
        </p>
      </div>
    </div>
  );
};
```

- [ ] **Step 2: Verificar tipos y lint**

Run: `cd packages/nextjs && yarn check-types && yarn lint`
Expected: sin errores

- [ ] **Step 3: Commit**

```bash
git add packages/nextjs/components/cursos/BotonDesbloquear.tsx
git commit -m "feat: añadir botón de desbloqueo de curso"
```

---

### Task 10: Página de detalle del curso

La pantalla donde se materializa el flujo completo del bounty.

**Files:**
- Create: `packages/nextjs/app/curso/[slug]/page.tsx`
- Create: `packages/nextjs/app/curso/[slug]/VistaCurso.tsx`

**Interfaces:**
- Consumes: `contentRepository` (Task 1), `useMembresia` (Task 5), todos los componentes de Tasks 6 y 9

- [ ] **Step 1: Crear el componente cliente con los dos estados**

Crear `packages/nextjs/app/curso/[slug]/VistaCurso.tsx`:

```typescript
"use client";

import { useState } from "react";
import { BotonDesbloquear } from "~~/components/cursos/BotonDesbloquear";
import { EstadoMembresia } from "~~/components/cursos/EstadoMembresia";
import { ListaModulos } from "~~/components/cursos/ListaModulos";
import { ReproductorVideo } from "~~/components/cursos/ReproductorVideo";
import { useMembresia } from "~~/hooks/useMembresia";
import type { Curso, Modulo } from "~~/types/curso";

type Props = {
  curso: Curso;
};

export const VistaCurso = ({ curso }: Props) => {
  const { estado, tieneAcceso, diasRestantes, lockAddress, isLoading, refetch } = useMembresia(curso.lockKey);
  const moduloGratuito = curso.modulos.find(m => m.esGratuito) ?? curso.modulos[0];
  const [moduloActivo, setModuloActivo] = useState<Modulo>(moduloGratuito);

  // El módulo que se puede reproducir: sin acceso, siempre el gratuito.
  const moduloReproducible = tieneAcceso ? moduloActivo : moduloGratuito;

  const seleccionarModulo = (modulo: Modulo) => {
    if (tieneAcceso || modulo.esGratuito) setModuloActivo(modulo);
  };

  if (isLoading) {
    return (
      <div className="container mx-auto px-4 py-10">
        <div className="skeleton h-8 w-72 mb-4" />
        <div className="skeleton aspect-video w-full" />
      </div>
    );
  }

  return (
    <div className="container mx-auto px-4 py-10">
      <header className="mb-6">
        <div className="flex flex-wrap items-center justify-between gap-3">
          <h1 className="text-3xl font-bold">{curso.titulo}</h1>
          <EstadoMembresia estado={estado} diasRestantes={diasRestantes} />
        </div>
        <p className="text-base-content/70 mt-1">
          Por {curso.creador.nombre} · {curso.creador.ciudad}
        </p>
      </header>

      <div className="grid gap-8 lg:grid-cols-[2fr_1fr]">
        <div className="space-y-6">
          <ReproductorVideo
            src={moduloReproducible.videoUrl}
            titulo={moduloReproducible.titulo}
            esVistaPrevia={!tieneAcceso}
          />
          <p>{curso.descripcion}</p>
        </div>

        <aside className="space-y-6">
          <ListaModulos
            modulos={curso.modulos}
            tieneAcceso={tieneAcceso}
            moduloActivoId={moduloReproducible.id}
            onSeleccionar={seleccionarModulo}
          />

          {!tieneAcceso && <BotonDesbloquear curso={curso} lockAddress={lockAddress} onCompraExitosa={refetch} />}
        </aside>
      </div>
    </div>
  );
};
```

> **Gating real:** cuando `tieneAcceso` es `false`, `moduloReproducible` es
> siempre el módulo gratuito. Las URLs de los módulos de pago no llegan al
> reproductor. No se oculta nada con CSS.

- [ ] **Step 2: Crear la página (Server Component)**

Crear `packages/nextjs/app/curso/[slug]/page.tsx`:

```typescript
import { notFound } from "next/navigation";
import { VistaCurso } from "./VistaCurso";
import { contentRepository } from "~~/services/content";

type Props = {
  params: Promise<{ slug: string }>;
};

const PaginaCurso = async ({ params }: Props) => {
  const { slug } = await params;
  const curso = await contentRepository.obtenerCursoPorSlug(slug);

  if (!curso) notFound();

  return <VistaCurso curso={curso} />;
};

export default PaginaCurso;
```

- [ ] **Step 3: Verificar tipos y lint**

Run: `cd packages/nextjs && yarn check-types && yarn lint`
Expected: sin errores

- [ ] **Step 4: Verificar en el navegador**

Run: `yarn start`
Expected: en `/curso/ingles-basico` se ve el módulo gratuito reproducible, los
demás con candado, y el botón de desbloqueo.

- [ ] **Step 5: Commit**

```bash
git add packages/nextjs/app/curso
git commit -m "feat: añadir página de detalle de curso con gating"
```

---

### Task 11: Desplegar Locks y conectar direcciones

Tarea con dependencia externa. Se puede ejecutar en paralelo a las tareas 5-10.

**Files:**
- Create: `packages/nextjs/.env.local` (no se commitea)
- Modify: `packages/nextjs/contracts/externalContracts.ts`

- [ ] **Step 1: Obtener fondos de testnet**

Conseguir ETH de Base Sepolia en un faucet público
(https://www.alchemy.com/faucets/base-sepolia). Se necesita muy poco.

- [ ] **Step 2: Desplegar los tres Locks**

En https://app.unlock-protocol.com — conectar la wallet, elegir la red
**Base Sepolia**, y crear tres Locks:

| Lock | Duración | Precio | Cantidad |
| ---- | -------- | ------ | -------- |
| Inglés desde cero | 30 días | 0.0001 ETH | Ilimitada |
| Excel para tu negocio | 30 días | 0.0001 ETH | Ilimitada |
| Reparación de celulares | 30 días | 0.0002 ETH | Ilimitada |

**Verificar en la configuración de cada Lock que las transferencias estén
habilitadas** — es lo que permite la funcionalidad P1.

- [ ] **Step 3: Guardar las direcciones**

Crear `packages/nextjs/.env.local` con las direcciones obtenidas:

```
NEXT_PUBLIC_LOCK_INGLES=0x...
NEXT_PUBLIC_LOCK_EXCEL=0x...
NEXT_PUBLIC_LOCK_CELULARES=0x...
```

Verificar que `.env.local` está ignorado por git:

```bash
git check-ignore packages/nextjs/.env.local && echo "ignorado correctamente"
```

- [ ] **Step 4: Registrar los Locks para la página /debug**

Reemplazar el contenido de `packages/nextjs/contracts/externalContracts.ts`:

```typescript
import { PUBLIC_LOCK_ABI } from "./unlock/publicLockAbi";
import type { GenericContractsDeclaration } from "~~/utils/scaffold-eth/contract";

/**
 * Los Locks se resuelven dinámicamente en contracts/unlock/locks.ts.
 * Se declaran aquí además para poder inspeccionarlos desde la página /debug.
 */
const externalContracts = {
  84532: {
    LockIngles: {
      address: (process.env.NEXT_PUBLIC_LOCK_INGLES ?? "0x") as `0x${string}`,
      abi: PUBLIC_LOCK_ABI,
    },
  },
} as const;

export default externalContracts satisfies GenericContractsDeclaration;
```

- [ ] **Step 5: Verificar la conexión onchain**

Run: `yarn start`, conectar la wallet a Base Sepolia, y comprar el acceso al
curso de inglés.
Expected: la transacción se confirma, el contenido completo aparece **sin
recargar la página**, y el badge muestra "Acceso activo · vence en 29 días".

- [ ] **Step 6: Commit**

```bash
git add packages/nextjs/contracts/externalContracts.ts
git commit -m "feat: registrar Locks desplegados en Base Sepolia"
```

---

### Task 12: Verificación manual del flujo P0

No produce código. Valida que el flujo obligatorio del bounty funciona
completo antes de pasar a P1.

**Files:**
- Create: `docs/verificacion-manual.md`

- [ ] **Step 1: Ejecutar los escenarios y documentar resultados**

Crear `docs/verificacion-manual.md` y completar la columna de resultado
ejecutando cada escenario en el navegador:

```markdown
# Verificación manual — Qupuy

Fecha de ejecución: ____
Red: Base Sepolia (84532)

| # | Escenario | Resultado esperado | ✓/✗ |
| - | --------- | ------------------ | --- |
| 1 | Abrir `/` sin wallet conectada | Se ven los 3 cursos, todos con badge "Sin acceso" | |
| 2 | Abrir `/curso/ingles-basico` sin wallet | Se reproduce el módulo gratuito; los demás con 🔒; aviso de conectar wallet | |
| 3 | Conectar wallet sin membresía | Aparece el botón "Desbloquear curso completo" con el precio | |
| 4 | Intentar reproducir un módulo bloqueado | El botón está deshabilitado; el video no cambia | |
| 5 | Comprar el acceso | La wallet pide firma; tras confirmar, el contenido completo aparece **sin recargar** | |
| 6 | Estado tras la compra | Badge "Acceso activo · vence en N días" | |
| 7 | Reproducir un módulo de pago | El video carga correctamente | |
| 8 | Recargar la página con acceso | Sigue mostrando el contenido completo | |
| 9 | Volver a `/` con acceso | La tarjeta del curso comprado muestra "Acceso activo" | |
| 10 | Cambiar a una red no soportada | Aviso claro de red incorrecta | |
| 11 | Cancelar la firma de compra | Mensaje "Cancelaste la compra"; la UI queda consistente | |
| 12 | Wallet sin fondos | Mensaje de error legible, sin pantalla rota | |
```

- [ ] **Step 2: Corregir lo que falle**

Cualquier escenario marcado ✗ se corrige antes de continuar. El flujo P0 debe
estar completo: es el requisito obligatorio del bounty.

- [ ] **Step 3: Commit**

```bash
git add docs/verificacion-manual.md
git commit -m "docs: añadir verificación manual del flujo P0"
```

---

# FASE P1 — Transferencia de acceso (el diferenciador)

> Solo empezar si las 12 tareas anteriores están cerradas y el escenario 5 de
> la verificación manual pasa.

---

### Task 13: Hook de transferencia

**Files:**
- Create: `packages/nextjs/hooks/useTransferirAcceso.ts`

**Interfaces:**
- Consumes: `PUBLIC_LOCK_ABI` (Task 2)
- Produces: `useTransferirAcceso(lockAddress, tokenId)` → `{ transferir: (destino: Address) => Promise<boolean>; isPending: boolean }`

- [ ] **Step 1: Implementar el hook**

Crear `packages/nextjs/hooks/useTransferirAcceso.ts`:

```typescript
"use client";

import { useState } from "react";
import type { Address } from "viem";
import { isAddress } from "viem";
import { useAccount, usePublicClient, useWriteContract } from "wagmi";
import { PUBLIC_LOCK_ABI } from "~~/contracts/unlock/publicLockAbi";
import { notification, getParsedError } from "~~/utils/scaffold-eth";

/**
 * Transferencia de una membresía a otra wallet.
 * La Key es un ERC-721: al transferirla, el remitente pierde el acceso.
 */
export const useTransferirAcceso = (lockAddress: Address | undefined, tokenId: bigint | undefined) => {
  const { address } = useAccount();
  const publicClient = usePublicClient();
  const { writeContractAsync } = useWriteContract();
  const [isPending, setIsPending] = useState(false);

  const transferir = async (destino: Address): Promise<boolean> => {
    if (!lockAddress || !address || tokenId === undefined) {
      notification.error("No tienes un acceso que puedas pasar");
      return false;
    }

    if (!isAddress(destino)) {
      notification.error("Esa dirección no es válida");
      return false;
    }

    if (destino.toLowerCase() === address.toLowerCase()) {
      notification.error("No puedes pasarte el acceso a ti mismo");
      return false;
    }

    setIsPending(true);
    try {
      const hash = await writeContractAsync({
        address: lockAddress,
        abi: PUBLIC_LOCK_ABI,
        functionName: "transferFrom",
        args: [address, destino, tokenId],
      });

      await publicClient?.waitForTransactionReceipt({ hash });
      notification.success("Acceso pasado correctamente");
      return true;
    } catch (error) {
      notification.error(getParsedError(error));
      return false;
    } finally {
      setIsPending(false);
    }
  };

  return { transferir, isPending };
};
```

- [ ] **Step 2: Verificar tipos**

Run: `cd packages/nextjs && yarn check-types`
Expected: sin errores

- [ ] **Step 3: Commit**

```bash
git add packages/nextjs/hooks/useTransferirAcceso.ts
git commit -m "feat: añadir hook de transferencia de acceso"
```

---

### Task 14: Modal de transferencia

**Files:**
- Create: `packages/nextjs/components/cursos/ModalTransferir.tsx`
- Modify: `packages/nextjs/app/curso/[slug]/VistaCurso.tsx`

**Interfaces:**
- Consumes: `useTransferirAcceso` (Task 13), `AddressInput` de `@scaffold-ui/components`
- Produces: `<ModalTransferir curso lockAddress tokenId onTransferencia />`

- [ ] **Step 1: Implementar el modal**

Crear `packages/nextjs/components/cursos/ModalTransferir.tsx`:

```typescript
"use client";

import { useState } from "react";
import { AddressInput } from "@scaffold-ui/components";
import type { Address } from "viem";
import { useTransferirAcceso } from "~~/hooks/useTransferirAcceso";
import type { Curso } from "~~/types/curso";

type Props = {
  curso: Curso;
  lockAddress: Address | undefined;
  tokenId: bigint | undefined;
  onTransferencia: () => void;
};

export const ModalTransferir = ({ curso, lockAddress, tokenId, onTransferencia }: Props) => {
  const [abierto, setAbierto] = useState(false);
  const [destino, setDestino] = useState("");
  const { transferir, isPending } = useTransferirAcceso(lockAddress, tokenId);

  const manejarTransferencia = async () => {
    const exitosa = await transferir(destino as Address);
    if (exitosa) {
      setAbierto(false);
      setDestino("");
      onTransferencia();
    }
  };

  return (
    <>
      <button type="button" className="btn btn-outline w-full" onClick={() => setAbierto(true)}>
        Pasar mi acceso a alguien →
      </button>

      {abierto && (
        <dialog className="modal modal-open">
          <div className="modal-box">
            <h3 className="font-bold text-lg">Pasar tu acceso</h3>

            <p className="py-3">
              Vas a pasarle tu acceso a <strong>{curso.titulo}</strong> a otra persona.
            </p>

            <div className="alert alert-warning">
              <span>Tú perderás el acceso. Solo una persona puede tenerlo a la vez.</span>
            </div>

            <div className="py-4">
              <label className="label">
                <span className="label-text">¿A quién se lo pasas?</span>
              </label>
              <AddressInput value={destino} onChange={setDestino} placeholder="0x… o nombre.eth" />
            </div>

            <div className="modal-action">
              <button type="button" className="btn btn-ghost" disabled={isPending} onClick={() => setAbierto(false)}>
                Cancelar
              </button>
              <button
                type="button"
                className="btn btn-primary"
                disabled={isPending || !destino}
                onClick={manejarTransferencia}
              >
                {isPending ? (
                  <>
                    <span className="loading loading-spinner" />
                    Pasando…
                  </>
                ) : (
                  "Pasar acceso"
                )}
              </button>
            </div>
          </div>
          <form method="dialog" className="modal-backdrop">
            <button type="button" onClick={() => setAbierto(false)}>
              cerrar
            </button>
          </form>
        </dialog>
      )}
    </>
  );
};
```

- [ ] **Step 2: Integrar el modal en la vista del curso**

En `packages/nextjs/app/curso/[slug]/VistaCurso.tsx`:

Añadir el import:

```typescript
import { ModalTransferir } from "~~/components/cursos/ModalTransferir";
```

Añadir `tokenId` a la desestructuración del hook:

```typescript
const { estado, tieneAcceso, diasRestantes, tokenId, lockAddress, isLoading, refetch } = useMembresia(curso.lockKey);
```

Reemplazar el bloque condicional del final del `<aside>`:

```typescript
{!tieneAcceso && <BotonDesbloquear curso={curso} lockAddress={lockAddress} onCompraExitosa={refetch} />}

{tieneAcceso && (
  <ModalTransferir curso={curso} lockAddress={lockAddress} tokenId={tokenId} onTransferencia={refetch} />
)}
```

- [ ] **Step 3: Verificar tipos y lint**

Run: `cd packages/nextjs && yarn check-types && yarn lint`
Expected: sin errores

- [ ] **Step 4: Probar la transferencia con dos wallets**

Con dos wallets en Base Sepolia: comprar el acceso con la wallet A,
transferirlo a la wallet B.
Expected: la wallet A pierde el acceso (el contenido vuelve a estado bloqueado
sin recargar) y la wallet B lo gana.

- [ ] **Step 5: Commit**

```bash
git add packages/nextjs/components/cursos/ModalTransferir.tsx packages/nextjs/app/curso
git commit -m "feat: añadir transferencia de acceso entre wallets"
```

---

### Task 15: Página "Mi acceso"

**Files:**
- Create: `packages/nextjs/app/mi-acceso/page.tsx`
- Create: `packages/nextjs/app/mi-acceso/ListaAccesos.tsx`
- Create: `packages/nextjs/components/cursos/TarjetaAcceso.tsx`

**Interfaces:**
- Consumes: `contentRepository` (Task 1), `useMembresia` (Task 5), `<ModalTransferir>` (Task 14)

- [ ] **Step 1: Crear la tarjeta de acceso**

Crear `packages/nextjs/components/cursos/TarjetaAcceso.tsx`:

```typescript
"use client";

import Link from "next/link";
import { EstadoMembresia } from "./EstadoMembresia";
import { ModalTransferir } from "./ModalTransferir";
import type { Curso } from "~~/types/curso";
import { useMembresia } from "~~/hooks/useMembresia";

type Props = {
  curso: Curso;
};

export const TarjetaAcceso = ({ curso }: Props) => {
  const { estado, tieneAcceso, diasRestantes, tokenId, lockAddress, refetch } = useMembresia(curso.lockKey);

  // Solo se listan los cursos con los que el usuario tuvo alguna relación.
  if (estado === "sin-acceso") return null;

  return (
    <div className="card bg-base-100 shadow-lg">
      <div className="card-body">
        <div className="flex flex-wrap items-center justify-between gap-2">
          <h2 className="card-title">{curso.titulo}</h2>
          <EstadoMembresia estado={estado} diasRestantes={diasRestantes} />
        </div>

        <p className="text-sm text-base-content/70">
          {curso.creador.nombre} · {curso.creador.ciudad}
        </p>

        <div className="card-actions mt-3 flex-col gap-2">
          <Link href={`/curso/${curso.slug}`} className="btn btn-primary w-full">
            {tieneAcceso ? "Ver curso" : "Volver a desbloquear"}
          </Link>

          {tieneAcceso && (
            <ModalTransferir curso={curso} lockAddress={lockAddress} tokenId={tokenId} onTransferencia={refetch} />
          )}
        </div>
      </div>
    </div>
  );
};
```

- [ ] **Step 2: Crear la lista**

Crear `packages/nextjs/app/mi-acceso/ListaAccesos.tsx`:

```typescript
"use client";

import Link from "next/link";
import { useAccount } from "wagmi";
import { TarjetaAcceso } from "~~/components/cursos/TarjetaAcceso";
import type { Curso } from "~~/types/curso";

type Props = {
  cursos: Curso[];
};

export const ListaAccesos = ({ cursos }: Props) => {
  const { address } = useAccount();

  if (!address) {
    return (
      <div className="alert alert-info">
        <span>Conecta tu wallet para ver tus accesos.</span>
      </div>
    );
  }

  return (
    <>
      <div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
        {cursos.map(curso => (
          <TarjetaAcceso key={curso.id} curso={curso} />
        ))}
      </div>

      <div className="text-center mt-10">
        <p className="text-base-content/70 mb-3">
          Los cursos que desbloquees aparecerán aquí.
        </p>
        <Link href="/" className="btn btn-outline">
          Ver todos los cursos
        </Link>
      </div>
    </>
  );
};
```

- [ ] **Step 3: Crear la página**

Crear `packages/nextjs/app/mi-acceso/page.tsx`:

```typescript
import type { NextPage } from "next";
import { ListaAccesos } from "./ListaAccesos";
import { contentRepository } from "~~/services/content";

const MiAcceso: NextPage = async () => {
  const cursos = await contentRepository.listarCursos();

  return (
    <div className="container mx-auto px-4 py-10">
      <header className="mb-8">
        <h1 className="text-3xl font-bold">Mis accesos</h1>
        <p className="text-base-content/70 mt-1">
          Tus cursos desbloqueados. Puedes pasarle cualquiera a otra persona.
        </p>
      </header>

      <ListaAccesos cursos={cursos} />
    </div>
  );
};

export default MiAcceso;
```

- [ ] **Step 4: Verificar tipos y lint**

Run: `cd packages/nextjs && yarn check-types && yarn lint`
Expected: sin errores

- [ ] **Step 5: Verificar en el navegador**

Run: `yarn start`
Expected: `/mi-acceso` sin wallet muestra el aviso de conectar. Con wallet y un
curso comprado, muestra esa tarjeta con el botón de pasar acceso.

- [ ] **Step 6: Commit**

```bash
git add packages/nextjs/app/mi-acceso packages/nextjs/components/cursos/TarjetaAcceso.tsx
git commit -m "feat: añadir página de accesos del usuario"
```

---

### Task 16: Verificación manual de P1

**Files:**
- Modify: `docs/verificacion-manual.md`

- [ ] **Step 1: Añadir y ejecutar los escenarios de transferencia**

Añadir al final de `docs/verificacion-manual.md`:

```markdown
## Fase P1 — Transferencia

| # | Escenario | Resultado esperado | ✓/✗ |
| - | --------- | ------------------ | --- |
| 13 | Wallet A con acceso abre el modal | Se ve la advertencia de pérdida de acceso | |
| 14 | Transferir a una dirección inválida | Mensaje "Esa dirección no es válida" | |
| 15 | Transferir a la propia dirección | Mensaje "No puedes pasarte el acceso a ti mismo" | |
| 16 | Transferir a la wallet B | Confirma; la wallet A pierde el acceso **sin recargar** | |
| 17 | Wallet B tras recibir | Tiene acceso completo al curso | |
| 18 | `/mi-acceso` en la wallet A | El curso transferido ya no aparece como activo | |
| 19 | Cancelar el modal | Se cierra sin efectos | |
```

- [ ] **Step 2: Corregir lo que falle**

- [ ] **Step 3: Commit**

```bash
git add docs/verificacion-manual.md
git commit -m "docs: añadir verificación manual de transferencia"
```

---

# FASE ENTREGA

---

### Task 17: README del proyecto

El bounty exige documentación que explique la integración con Unlock.

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Reescribir el README**

Reemplazar `README.md` con la documentación del proyecto. Debe contener:

1. **Qué es Qupuy** — una frase, y el significado quechua del nombre.
2. **El problema** — Bolivia no tiene PayPal ni MercadoPago; un creador local
   no puede cobrar por internet.
3. **Cómo se integra Unlock** (sección obligatoria del bounty):
   - Cada curso está atado a un Lock de Unlock.
   - El acceso se determina con `getHasValidKey`, no con `balanceOf`, porque
     `balanceOf` cuenta también las membresías vencidas.
   - La compra usa `purchase()` del PublicLock.
   - La transferencia usa `transferFrom()` — las Keys son ERC-721.
   - Rutas de los archivos clave: `hooks/useMembresia.ts`,
     `contracts/unlock/`, `hooks/useTransferirAcceso.ts`.
4. **Direcciones de los Locks desplegados** con enlaces al explorador.
5. **Cómo ejecutarlo localmente** — `yarn install`, `.env.local`, `yarn start`.
6. **Limitación conocida** — las URLs de video son descubribles vía DevTools;
   la ruta de producción son URLs firmadas validadas server-side.
7. **Declaración de código preexistente** — copiar de `CONTEXT.md` §10.

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: reescribir README con la integración de Unlock"
```

---

### Task 18: Despliegue público

El bounty exige una URL donde el jurado pueda probar el proyecto.

- [ ] **Step 1: Desplegar en Vercel**

Run: `yarn vercel`

Configurar las variables de entorno en el panel de Vercel:
`NEXT_PUBLIC_LOCK_INGLES`, `NEXT_PUBLIC_LOCK_EXCEL`,
`NEXT_PUBLIC_LOCK_CELULARES`.

- [ ] **Step 2: Verificar el despliegue**

Abrir la URL pública y repetir los escenarios 1, 2, 3 y 5 de
`docs/verificacion-manual.md`.
Expected: el flujo completo funciona igual que en local.

- [ ] **Step 3: Registrar la URL**

Añadir la URL pública al README y a `CONTEXT.md` §13 (tabla de entregables).

- [ ] **Step 4: Commit**

```bash
git add README.md CONTEXT.md
git commit -m "docs: añadir URL pública del despliegue"
```

---

### Task 19: Cierre de entregables

- [ ] **Step 1: Completar la declaración de código preexistente**

En `CONTEXT.md` §10, listar todos los archivos creados durante la hackathon
en la sección "Código ORIGINAL". Revisar con:

```bash
git log --oneline --name-only --diff-filter=A | sort -u
```

- [ ] **Step 2: Verificar la lista de entregables del bounty**

| Entregable | Dónde |
| ---------- | ----- |
| Nombre del proyecto | Qupuy |
| Repositorio público | URL del repo |
| README con la integración de Unlock | `README.md` |
| URL pública probable | Vercel |
| Video demo ≤ 3 min | Por grabar |
| Direcciones de los Locks | `README.md` |
| Declaración de código preexistente | `CONTEXT.md` §10 |

- [ ] **Step 3: Actualizar el handoff**

Actualizar `CONTEXT.md` §7 con el estado final.

- [ ] **Step 4: Commit**

```bash
git add CONTEXT.md
git commit -m "docs: cerrar entregables del bounty"
```

---

## Guion sugerido para el video (≤ 3 min)

No es una tarea de código, pero el video es un entregable evaluado.

| Tiempo | Contenido |
| ------ | --------- |
| 0:00-0:25 | **El problema.** Bolivia es el único país de LatAm sin PayPal; MercadoPago tampoco opera. Un profesor acá no puede cobrar por internet. |
| 0:25-0:45 | **Qupuy.** Catálogo, y qué significa el nombre en quechua: dar a otro, y también pagar. |
| 0:45-1:15 | **Descubrir y previsualizar.** Entrar a un curso sin wallet: el módulo gratuito se reproduce, los demás con candado. |
| 1:15-1:55 | **Verificar y desbloquear.** Conectar wallet, comprar, y mostrar cómo el contenido aparece solo — Unlock decide el acceso. |
| 1:55-2:35 | **El diferenciador.** Pasar el acceso a otra wallet. Mostrar que el remitente lo pierde y el receptor lo gana. |
| 2:35-3:00 | **Cierre.** "Ninguna plataforma permite prestar un curso. Aquí el acceso te pertenece — y cuando terminas, lo pasas." |
